{
	"scripts": {
		"publish-dry-run": "make test && make version && make build && make release-dry-run",
		"publish": "make test && make version && make build && make release"
	},
	"devDependencies": {
		"esbuild": "^0.11.6",
		"react": "^17.0.2",
		"react-dom": "^17.0.2"
	}
}
