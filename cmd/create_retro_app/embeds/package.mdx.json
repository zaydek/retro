{
	"scripts": {
		"dev": "retro dev",
		"build": "retro build",
		"serve": "retro serve"
	},
	"dependencies": {
		"@mdx-js/react": "{{ .MDXVersion }}",
		"react": "{{ .ReactVersion }}",
		"react-dom": "{{ .ReactVersion }}"
	},
	"devDependencies": {
		"@mdx-js/mdx": "{{ .MDXVersion }}",
		"@zaydek/retro": "{{ .RetroVersion }}",
		"esbuild": "{{ .EsbuildVersion }}"
	}
}
