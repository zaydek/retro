{
	"scripts": {
		"dev": "retro dev",
		"build": "retro build",
		"serve": "retro serve"
	},
	"dependencies": {
		"react": "{{ .ReactVersion }}",
		"react-dom": "{{ .ReactDOMVersion }}"
	},
	"devDependencies": {
		"esbuild": "{{ .EsbuildVersion }}"
	}
}
