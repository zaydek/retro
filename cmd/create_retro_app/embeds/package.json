{
	"scripts": {
		"dev": "retro dev",
		"build": "retro build",
		"serve": "retro serve"
	},
	"dependencies": {
		"react": "{{ .ReactVersion }}",
		"react-dom": "{{ .ReactVersion }}"
	},
	"devDependencies": {
		"@zaydek/retro": "{{ .RetroVersion }}",
		"esbuild": "{{ .EsbuildVersion }}"
	}
}
