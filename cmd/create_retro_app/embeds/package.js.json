{
	"scripts": {
		"dev": "retro dev",
		"build": "retro build",
		"serve": "retro serve"
	},
	"dependencies": {
		"react": "{{ .ReactVersion }}",
		"react-dom": "{{ .ReactDOMVersion }}"
	},
	"devDependencies": {
		"@zaydek/retro": "{{ .RetroVersion }}",
		"esbuild": "{{ .EsbuildVersion }}"
	}
}
