{
	"scripts": {
		"dev": "retro dev",
		"build": "retro build",
		"serve": "retro serve"
	},
	"dependencies": {
		"react": "{{ .React }}",
		"react-dom": "{{ .React }}"
	},
	"devDependencies": {
		"@zaydek/retro": "{{ .Retro }}",
		"esbuild": "{{ .Esbuild }}",
		"sass": "{{ .Sass }}"
	}
}
